[package]
name = "verify-attestation-rs"
version = "0.1.0"
edition = "2021"
description = "Example: call verify-attestation Go binary from Rust and inspect claims"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
